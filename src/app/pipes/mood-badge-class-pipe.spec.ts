import { MoodBadgeClassPipe } from './mood-badge-class-pipe';

describe('MoodBadgeClassPipe', () => {
  it('create an instance', () => {
    const pipe = new MoodBadgeClassPipe();
    expect(pipe).toBeTruthy();
  });
});
